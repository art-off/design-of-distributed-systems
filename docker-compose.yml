version: "3.8"

services:

  monitor:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: node build/monitoring/index.js
    ports:
      - "3000:3000"
    depends_on:
      - peer

  peer:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: node build/peer/index.js
    deploy:
      replicas: 3