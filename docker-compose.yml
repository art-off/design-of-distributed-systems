version: "3.8"

services:

  monitor:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: node build/monitoring/index.js
    depends_on:
      - peer

  peer:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: node build/peer/index.js
    deploy:
      replicas: 3